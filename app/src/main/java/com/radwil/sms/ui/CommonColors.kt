package com.radwil.sms.ui

import com.radwil.sms.App
import radwil.sms.R

object CommonColors {
   val accentColor = App.resources.getColor(R.color.colorAccent)
   val transparent = App.resources.getColor(R.color.transparent)
   val lightGray = App.resources.getColor(R.color.lightGray)
   val gray = App.resources.getColor(R.color.gray)
}